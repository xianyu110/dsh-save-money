# Changelog

本文件记录 dsh-save-money 的每一次版本变化:你能得到什么新能力、操作方式有什么变化。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/),版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [1.4.3] — 2026-08-18

### 新增

- **自动化发布脚本**（`scripts/release.js`，零依赖）：一条命令覆盖所有版本触点——界面版本（根 `package.json`，构建时注入客户端页脚）、`plugin/package.json`、两份 README 里的 `dsh-save-money-<x.y.z>.tgz` 示例、测试断言、`v<版本>` tag、推送与 `npm publish`。`--dry-run` 演练；代理用 `--proxy http://host:port` 传入。
- **npm 包现在携带 README**：发布步骤会把根 `README.md` 临时复制到 `plugin/README.md`（npm 只打包 `plugin/` 目录内文件）并在发布后立即移除——仓库始终只有根目录一份 README。

### 变更

- `plugin/package.json` 的 `files` 清理：移除无效的 `"../README.md"` / `"../README.zh.md"` 条目（npm 忽略包目录外路径）；`plugin/README.md` 已加入 .gitignore。

## [1.4.2] — 2026-08-18

### 变更

- **代码库重构（内部,行为不变）**：把超大的单文件拆成小而聚焦的模块——`src/client.ts`（1730 → 259 行）只保留数据层与插槽接线,UI 移到 `src/ui/*`（徽章 / 悬浮横幅 / 设置面板 / 柱状图 / 头部入口 + 组合根）;`src/host.ts`（1234 → 400 行）只负责接线子模块（`host-goals` / `gate` / `balance-tracker` / `host-http` / `host-tools`）;余额逻辑拆到 `balance-history` / `balance-bars` / `balance-host`（传输层）;配置控制器与纯状态机分别在 `config.ts` / `state.ts`;UI 文案字典移到 `src/i18n/*` 按语言分文件（10 个语言文件 + 类型 + 聚合器）。每个源文件不超过 400 行,所有代码注释均为英文（用户可见的 i18n 文案除外）。105 个单元测试全部通过。
- **设置面板布局更紧凑**：「启用」与「显示余额」并排一行;模型档位改为两行——**官方 API：flash / pro**、**opencode go·zen API：flash / pro**——不再是四个开关各占一行（10 种语言同步更新）。

### 修复

- **充值不再扭曲消费柱状图**：余额回升（充值/退款）窗口不再画负柱、不再拉伸 Y 轴——Y 轴只保留正向（消费）;被隐藏的回升窗口悬停时标注「余额回升（充值/退款）,未计入消费分析」。
- **余额历史真正持久化（回归修复）**：5 分钟写盘节流与卸载落盘此前门控在一个从未被赋值的脏标记上,采样历史只存在于内存、每次重启即丢失。现在脏标记放到共享插件状态上,且只在**写成功后**清除——写失败会保留标记等下个周期重试。
- **凭据瞬时失败不再清空持久化历史**：启动时 API key 解析失败,已加载的历史会保留、稍后再校验;文件永远不会被占位指纹覆盖。
- **保存设置不再丢失 `days` 与每窗口时区**：设置 UI 现在把这些字段带入窗口行;下拉时区只作用于没有显式时区的窗口。
- **配置校验加固**：`enabled` / `reconcileOnStart` 做布尔类型校验（`"yes"` 这类 truthy 字符串被拒绝）,未知字段直接丢弃、不再混入运行时配置。
- **配置变更立即唤醒挂起请求**：禁用或修改窗口会马上释放闸门等待者（此前最多要等 60 秒的 tick）。
- **重新启用即重置跳过的窗口**：取消再勾选「启用」会清除一次性的「结束本次省钱模式」状态,所有窗口重新生效。
- **闸门双重释放防护**：中止信号与唤醒循环同一瞬间命中同一挂起请求时,下游链路可能被调用两次;现已加 once 保护。
- **客户端轮询去重**：每个挂载组件一个 30 秒状态轮询,不再叠加 apply 级额外轮询（减少冗余请求）。
- **i18n 一致性由测试保障**：新增测试断言所有语言字典 key 集合一致且无重复,翻译遗漏不再静默回退英文。

## [1.4.1] — 2026-08-18

### 新增

- **按模型档位决定是否省钱（只对你想暂停的模型生效）**：设置面板新增「适用模型」区块，4 个独立勾选——官方 flash / 官方 pro / opencode go·zen flash / opencode go·zen pro。勾选的档位在暂停窗口内**执行省钱**（挂起请求）；不勾选的档位**豁免**（即使窗口内也照常放行）。请求按 provider 路由名（`deepseek-official` 或名称含 `opencode` 的 provider）和模型名（`flash`/`pro`）实时识别。
- **合理的默认值 + 尊重用户选择**：全新安装默认勾选官方 flash/pro（窗口内暂停），opencode go/zen 默认不勾选（OpenCode 是订阅套餐,想暂停时再自己勾）。你改过任何勾选后,持久化保存,之后绝不重置。
- **安全兜底**：任何无法识别为官方 flash/pro 或 opencode flash/pro 的模型——旧名（`deepseek-chat`/`deepseek-reasoner`）、其他第三方、其他任何模型——一律**豁免**（绝不暂停）,所以识别不到的 provider 永远不会阻塞你的请求。
- **「一键 DeepSeek 预设」按钮**移到暂停窗口列表正上方（它是窗口的快捷操作,不是通用设置）。

## [1.4.0] — 2026-08-18

### 新增

- **消费柱形图（最近 8 小时、每 10 分钟一根）**：点击头部余额数字弹出图表窗口。48 根柱,正数=消费（红色、向上）、负数=充值/退款（绿色、向下）、0=中性、未采样=浅色点。零图表库依赖,纯主题 token,10 种语言。带 **X 轴**（8 个整点小时刻度,按配置时区）和 **Y 轴**（1/2/5×10ⁿ 关键点刻度,≤5 个,带虚线网格线;上限略高于最高柱,便于读数）。
- **采样与窗口时间对齐**：采样点对齐整 5 分钟、柱窗口对齐整 10 分钟,**按配置时区**对齐（夏令时感知,含 +5:45 这类非整小时偏移,如尼泊尔）——与官方后台按当地整点计费一致,图表可直接和官方账单对账。悬停余额卡片的消费统计也标注精确时间窗口（如「近1h消费 07:00–08:00 ¥1.25」）,与柱形图口径统一。
- **余额历史持久化**到 `~/.dsh/dsh-save-money-balance.json`（账户级、跨项目共享）:插件更新 / 重启 / 停用再启用后,已采样的历史不再丢失。文件以 DeepSeek API key 的指纹标识——**更换 key（= 更换账户）自动作废旧历史**,不会把旧账户的余额轨迹带到新账户。
- **外部消费归因（不再"1 秒消费爆炸"惊吓）**：插件按采样点记录本环境是否真的产生过模型请求（`llm/stream` 活动信号）。柱形图上,**本环境无活动但余额下降**的窗口渲染为**警示色柱**,悬停提示"本窗口无本环境活动,变动可能来自其他环境"——不再当成本环境消费。
- **图表底部免责声明**：「数据为采样计算获得仅供参考,实际以官方数据为准」（10 种语言）。

### 修复

- **柱形图假 0 柱**：窗口两端取到同一个采样点（历史覆盖不足）时显示为 `null`（浅点）,绝不伪装成"0 消费"。
- **柱形图交互**：点击余额改为开/关切换并清除悬停卡片;真 0 值用中性色而非红色;标题不再重复;打开柱形图时强制刷新,保证图表是最新采样。
- **统一窗口时钟**：1h/10m/24h 消费统计与柱形图共用同一对齐时钟基准,悬停卡片与图表指向同一段时间（此前可能差几分钟,对不上）。

## [1.3.3] — 2026-08-17

### 修复

- **余额显示跟随实际使用的模型(多模型源场景)**:配置了多个模型源(DeepSeek 官方 + 硅基流动、中转等)时,只要会话的默认模型不是官方 DeepSeek,余额之前就会消失。现在插件读取**最近一次真实模型请求的 provider**(`llm/stream`):最近请求跑在 `deepseek-official` 上就显示余额,否则隐藏。隐藏时**不会清除**已采样的消费统计,切回 DeepSeek 后余额(及其消费数据)立即恢复显示。
- **官方 API 闸门不再依赖默认模型**:此前当 `agentDefaultModel.currentSelection()` 不是 `deepseek-official` 时闸门直接拒绝——这是错误信号,因为余额属于官方账户,与当前默认选择无关。只有把 `llm-deepseek` 的 baseURL 改成非官方端点(没有 `/user/balance` 的中转)时才会禁用。

## [1.3.2] — 2026-08-17

### 新增

- **设置界面显示插件名称与版本号**:设置浮层底部(保存按钮左侧)新增 `save-money v1.3.2` 文本;版本号在构建时从 `package.json` 自动注入,始终与发布的清单一致。

### 修复

- **配置持久化不再依赖插件激活顺序**:在部分机器上,插件行可能先于 DSH 的 `fs` 服务被激活(加载顺序与开发机不同),启动时一次性读取的 `ctx.get('fs')` 拿到的是 `undefined`,导致设置既不保存也不加载——表现为"保存了设置,重启后却恢复默认"。现在插件在每次使用时动态读取 `fs` 服务,并通过 `ctx.inject(['fs'])` 等待其就绪,晚到的 `fs` 一出现就会被拾取。

## [1.3.1] — 2026-08-17

### 修复

- **配置文件位置解析更可靠**:不再依赖"恰好有一个会话的工作区指向本仓库"。现在按候选列表自动查找(指针文件 → 当前会话工作区 → 仓库目录 → DSH 启动目录 → 启动目录的同级 `dsh-save-money` 目录 → 兜底),**第一个存在配置文件的目录胜出**;写入成功后自动在 `~/.dsh/` 记录指针,重启后从同一位置加载。
- **全新安装不再把配置写进 DSH 安装目录**:任何位置都没有配置文件时,优先写到仓库目录(README 安装教程的 `~/app/` 并列布局也能命中),而不是污染 harness 安装目录;彻底消除"设置改了却找不到 / 重启后配置丢失"的问题。

## [1.3.0] — 2026-08-17

### 新增

- **账户余额显示**（DeepSeek 官方 API）：右上角状态文字旁显示当前余额，货币符号自动识别（¥ / $ / €…），颜色随亮暗主题自适应。默认关闭，在设置里勾选「显示余额」开启。
- **余额自动刷新**：每次你发送消息时立即更新一次；平时每 5 分钟自动更新一次。
- **消费统计**：后端每 5 分钟采样一次余额（288 个点覆盖最近 24 小时），悬停余额即可看到**最近 1 小时 / 10 分钟 / 24 小时消费了多少钱**；余额回升（如充值、退款）显示为「+金额」。
- **自定义悬浮卡片**：余额明细与消费统计改为多行悬浮卡片，跟随鼠标、从光标处向左展开，永远不会超出屏幕边缘（替换原先会被视口截断的浏览器原生提示）。

## [未发布] — 1.2.5

### 修复

- **官方安装方式下「启用」开关（以及所有设置项）现在可以正常点击生效**：部分部署环境中插件启动早于 Web 服务就绪，界面请求无法到达插件，点击看似无反应。现在插件会等待 Web 服务可用后再注册自己的接口——与动态插件方式行为一致。

## [未发布] — 1.2.4

### 修复

- 官方安装包现在能正确加载浏览器界面：1.2.3 的包清单缺少 `./package.json` 导出声明，导致 DSH 无法发现包内自带的界面，安装后看不到状态文字 / 横幅 / 设置页。1.2.4 起安装后界面自动加载（装完刷新一次浏览器页面即可）。

## [未发布] — 1.2.3

### 新增

- **官方安装方式现在自带完整界面**(状态文字、浮动横幅、设置页):浏览器启动时自动加载插件界面,用 `dsh plugin add`(或 `--patch`)安装后就是完整插件——不再需要动态插件步骤或 AI 辅助安装。动态插件方式保持原有行为不变。

## [未发布] — 1.2.2

### 修复

- 官方安装方式（`--patch` / 安装包）在没有动态插件沙箱的机器上不再启动失败：工具改由 DSH 标准机制注册,插件在任何部署环境（如树莓派）都能正常加载使用；动态插件方式不受影响。

## [未发布] — 1.2.1

### 新增

- **切换时区自动换算窗口时间**:更改时区下拉后,所有窗口时间自动换算(真实时区规则,夏令时感知;北京 08:58 夏季对应伦敦 01:58、冬季 00:58),与「保存」按钮一起生效。
- **24 个整数时区**下拉(UTC-11 … UTC+12):每个偏移一个代表时区,并实时标注当前 UTC 偏移——不再是 400 项长列表。
- **10 种语言**:简体中文、繁體中文、English、Deutsch、Français、Español、Italiano、Português、日本語、한국어——跟随浏览器自动识别,也可在设置中手动选择。

### 修复

- 修正时区偏移计算(含 ±12/13 小时边界,如新西兰),所有时区显示与换算准确。

## [1.2.0] — 2026-08-16

### 新增

- **「结束本次省钱模式」按钮**(顶部横幅与设置界面都有):点击只结束**当前这一个**暂停窗口——
  - 已经暂停:立即恢复,继续正常对话;
  - 即将暂停:取消本次暂停;
  - 只跳过当前窗口,下一个窗口(今天或以后)照常生效;
  - **绝不会关掉「启用」开关**——不用担心点了之后忘记重新启用,以后不再省钱。
- **随时重置**:把「启用」开关取消勾选、再重新勾选,所有窗口立刻重新生效(之前被跳过的窗口也恢复)。
- **设置界面的绿色提示**:当有窗口被跳过时,会显示绿字告诉你——哪个窗口被跳过了、几点自动恢复省钱、以及如何重置。
- **默认时间表**:新安装、或窗口全部删空时,设置界面直接预填 DeepSeek 高峰时间表(**08:58–12:02、13:58–18:02**,含 2 分钟边界余量),不用再手动输入。
- **一键 DeepSeek 所见即所得**:按钮按设置界面里**你当前看到**的窗口列表来升级(没保存的修改也会被尊重),结果立即显示在列表中。
- 插件面板显示版本号(**save-money v1.2.0**);README 增加了通俗易懂的「工作原理」示意图。

### 改进

- 按钮文字从「停用省钱模式」改为「结束本次省钱模式」:把"只跳过这一次"和"彻底关闭省钱功能"明确区分开,不再容易混淆。
- 取消即将到来的暂停后,页面不再残留"即将暂停"的黄色提醒。
- 设置始终保存到你当前正在使用的项目目录,不会再出现"改了设置却找不到"的困惑。

### 修复

- 修复一个边界情况:跳过某个窗口后,如果另一个时区不同的窗口恰好重叠,请求不再被误放行——该暂停的窗口依然会暂停。

## [1.1.0] — 2026-08-15

### 新增

- **暂停窗口内不产生任何费用**:窗口期间 AI 不会发出任何新请求;窗口结束自动恢复,对话上下文和进行中的任务都不受影响(窗口内 AI 不回复是预期行为)。
- **到点自动暂停**:暂停时刻到达后,正在运行的任务被安全"冻住"(进度原样保留),窗口结束自动接着跑;**没有任务在跑就不暂停**。
- **多组时间窗口**:暂停/继续时间自由增删,支持跨午夜(23:00–08:00)、按星期过滤。
- **界面提醒**:顶部浮动横幅(即将暂停浅黄 / 已暂停浅红)+ 会话头部常驻彩色状态入口,颜色随状态实时变化。
- **一键 DeepSeek 高峰省钱策略**:自动添加带 2 分钟边界余量的高峰窗口(08:58–12:02、13:58–18:02),不自动启用,由你决定;旧版无余量窗口会自动升级。
- **时区支持**:浏览器自动探测,可手动选择(默认北京时间)。
- **设置持久化**:所有设置自动保存到工作区文件,刷新页面、重启后依然保留。
- **中英文界面**:跟随浏览器语言自动切换,也可手动指定。
- **三种安装方式**:快速试用、正式安装包、开发者调试形态。

### 说明

- 这是 git 仓库中第一个有记录的版本;更早的版本没有保留提交历史。

## [1.0.x] — git 历史之前

- 最初的实现(开发者调试形态):时间窗口自动暂停/恢复;"停用省钱模式"按钮与"跳过暂停"行为(1.2.0 起改为「结束本次省钱模式」);无边界余量的 DeepSeek 窗口(一键升级时会自动替换为带余量版本)。
