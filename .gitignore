# Runtime config persisted by the plugin (never committed)
save-money.config.json

# Third-party reference copy of the DSH source (never committed)
/deepseek-harness-0.1/

# Build output
node_modules/
dist/

# npm pack artifacts (plugin/dsh-save-money-*.tgz)
*.tgz

# Temporary README copied into plugin/ only during npm publish (removed after)
plugin/README.md

# Logs, editor/OS droppings
*.log
.DS_Store
Thumbs.db
